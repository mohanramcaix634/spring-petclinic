name: Java CI with Maven
on:
  push:
    branches: [ main ]
jobs:
  Build-Maven:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
        name: Checkout
      - name: Build Maven
        run: 'whoami'
