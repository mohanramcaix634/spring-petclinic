name: Java CI with Maven
on:
  push:
    branches: [ main ]
jobs:
  Build-Maven:
    runs-on: self-hosted
    strategy:
      matrix:
        java: [ '17' ]

    steps:
      - uses: actions/checkout@v2
      - name: Build with Maven Wrapper
        run: ./mvnw -B package
